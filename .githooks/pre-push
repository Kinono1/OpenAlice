#!/usr/bin/env bash
set -euo pipefail

REPO_ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
cd "$REPO_ROOT"

node scripts/branch_policy_enforce_push.mjs --remote-name "${1:-}" --remote-url "${2:-}"
